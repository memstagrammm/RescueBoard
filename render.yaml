services:
  - type: web
    name: rescueboard
    env: python
    buildCommand: "pip install -r requirements.txt && python manage.py collectstatic --noinput && python manage.py migrate"
    startCommand: "gunicorn board_project.wsgi:application --bind 0.0.0.0:$PORT"
    rootDir: .
